import React from 'react';

const Cancel = () => {
    return (
        <div>
            <h2>Payment Canceled</h2>
            <p>Your payment has been canceled. You can go back to the cart and try again.</p>
        </div>
    );
};

export default Cancel;
